# Data on the Web 
# With the HTTP request/response well understood and well supportedm, there was a natural move toward exhacnging data between prgrams using these protocols
# We need to come up with an agreed way to represent data going between applications and across networks
# There are two commonly used for formats : XML and JSON

#Sending Data across the "Net"

# python dictionaty                      Java HashMap
      # ---->              <person>            ---->
      # Serialize           <name>        De-Serialize

# XML = Marking up data to sending across the network...